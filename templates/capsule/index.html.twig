{% extends 'base.html.twig' %}

{% block title %}Mes Time Capsules{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>🎁 Mes Capsules Temporelles</h1>

    {# LE VIDEUR : On vérifie si l'utilisateur a confirmé son email #}
    {% if app.user and not app.user.isVerified %}

        <div class="alert alert-warning text-center mt-5">
            <h4>📧 Vérification requise !</h4>
            <p>Merci de votre inscription.</p>
            <p>Pour voir et créer des capsules, vous devez d'abord <strong>cliquer sur le lien</strong> reçu par email.</p>
            <p class="small text-muted">Vérifiez vos spams si vous ne voyez rien.</p>
        </div>

    {% else %}

        {# ======================================================= #}
        {# 1. LE DASHBOARD (Compte à rebours) - MAINTENANT ICI !   #}
        {# ======================================================= #}
        {% if nextCapsule %}
            <div class="card mb-4 border-primary shadow-sm mt-4">
                <div class="card-body text-center bg-light">
                    <h4 class="text-primary"><i class="fas fa-hourglass-half"></i> Prochain Voyage Temporel</h4>

                    <p class="lead mt-3">
                        La capsule <strong>"{{ nextCapsule.title }}"</strong> décollera vers
                        <em>{{ nextCapsule.targetEmail }}</em> le :
                    </p>

                    <div class="display-6 fw-bold text-dark">
                        {{ nextCapsule.sendDate|date('d/m/Y') }} à {{ nextCapsule.sendDate|date('H:i') }}
                    </div>

                    <p class="text-muted mt-2" id="countdown-text">Calcul du temps restant...</p>

                    <script>
                        var targetDate = new Date("{{ nextCapsule.sendDate|date('Y-m-d H:i:s') }}").getTime();

                        var x = setInterval(function() {
                            var now = new Date().getTime();
                            var distance = targetDate - now;

                            var days = Math.floor(distance / (1000 * 60 * 60 * 24));
                            var hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
                            var minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));

                            if (distance < 0) {
                                clearInterval(x);
                                document.getElementById("countdown-text").innerHTML = "🚀 Décollage imminent !";
                            } else {
                                document.getElementById("countdown-text").innerHTML =
                                    "⏱️ Décollage dans : " + days + "j " + hours + "h " + minutes + "m ";
                            }
                        }, 1000);
                    </script>
                </div>
            </div>
        {% endif %}
        {# ======================================================= #}


        {# 2. LE TABLEAU DES CAPSULES #}
        <div class="text-end mb-3 mt-4">
            <a href="{{ path('app_capsule_new') }}" class="btn btn-primary">Créer une nouvelle capsule</a>
        </div>

        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Titre</th>
                    <th>Date d'envoi</th>
                    <th>Statut</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for capsule in capsules %}
                <tr>
                    <td>{{ capsule.title }}</td>
                    <td>{{ capsule.sendDate ? capsule.sendDate|date('Y-m-d H:i') : '' }}</td>
                    <td>
                        {% if capsule.isSent %}
                            <span class="badge bg-success">Envoyée</span>
                        {% else %}
                            <span class="badge bg-secondary">En attente</span>
                        {% endif %}
                    </td>
                    <td>
                        <a href="{{ path('app_capsule_show', {'id': capsule.id}) }}" class="btn btn-sm btn-info">Voir</a>

                        {% if not capsule.isSent %}
                            <a href="{{ path('app_capsule_edit', {'id': capsule.id}) }}" class="btn btn-sm btn-warning">Modifier</a>
                            <form method="post" action="{{ path('app_capsule_delete', {'id': capsule.id}) }}" style="display: inline-block" onsubmit="return confirm('Vraiment supprimer ?');">
                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ capsule.id) }}">
                                <button class="btn btn-sm btn-danger">Supprimer</button>
                            </form>
                        {% else %}
                            <span class="text-muted small">🔒 Verrouillé</span>
                        {% endif %}
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="4" class="text-center">Vous n'avez aucune capsule pour le moment.</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

    {% endif %}
</div>
{% endblock %}
